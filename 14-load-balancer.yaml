apiVersion : v1
kind: Service
metadata:
  name: nginx-lb
spec:
  type: loadbalancer
  selector:
    project: expense
    module: backend
    enviroment: dev
  ports:
  - name: nginx-svc-port
    protocol: TCP
    port: 80 # service port
    targetPort: 80 #container port



