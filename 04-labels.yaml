apiVersion : v1
kind : pod
metadata :
  name : labels
  labels : # key value pairs
    project : expense
    module : backend
    enviroment : dev
  spec:
    containers :
    - name : nginx
      image : nginx