apiversion: v1
kind: pod
metadata :
  name: nginx
spec:
  containers:
  #docker run -d  --name nginx nginx
  - name: nginx
    image: nginx
  - name: almalinux
    image: almalinux:9
    command: ["sleep","100"]


  # can to pod have same name :- No 